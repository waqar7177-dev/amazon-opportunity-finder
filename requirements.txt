# Runtime dependencies. Chosen to install on locked-down Windows machines (Smart App Control /
# application-control policies) without compiling anything. Playwright ships a signed driver and uses
# the browser that is already installed; it was verified to run under Smart App Control.
Flask>=3.0,<4
openpyxl>=3.1,<4
waitress>=3.0,<4
python-dotenv>=1.0,<2
# Brand research: drives the installed Microsoft Edge / Google Chrome (no browser download on Windows).
playwright>=1.47,<2
beautifulsoup4>=4.12,<5
